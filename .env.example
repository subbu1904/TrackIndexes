# Copy this file to .env.local and fill in your values.
# .env.local is gitignored and will not be committed.

# Set to "true" to use MockProvider (fake data) instead of live Yahoo Finance data.
# Useful for development and offline testing. Defaults to live data when not set.
# Example: VITE_USE_MOCK=true
VITE_USE_MOCK=
